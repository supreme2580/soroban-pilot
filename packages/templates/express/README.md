# Express + Stellar Starter

Express API server with Stellar/Soroban integration pre-configured.

## Getting Started

```bash
pnpm install
pnpm dev
```

## Endpoints

- `GET /api/account/:address` - Account details
- `POST /api/transaction` - Submit transaction
- `GET /api/contract/:id` - Contract details
